import { NotFoundPage } from "@/components/templates/NotFoundPage/NotFoundPage";

// @to-do add meta data when nextjs team fix them and add ability to do that

export default function NotFound() {
  return <NotFoundPage />;
}
